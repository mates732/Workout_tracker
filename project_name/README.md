# project_name

Startup-style directory scaffold:
- `core/` architecture contracts
- `models/` domain aliases
- `services/` orchestration aliases
- `api/` route contracts
- `database/` repository stubs
- `ui/` theme and component registry
- `config/` runtime settings
- `tests/` smoke tests
